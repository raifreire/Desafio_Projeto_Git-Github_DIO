```markdown
# Primeiro passo criar repositório no github 

# segundo passo clonar o repositorio na maquina local.  
	- copie o codigo ssh, depois crie a pasta do 
	  projeto, depois git bash here, git clone e 
	  cole o codigo ssh copiado.
# Após fazer alterações volte no git bash
		- comando git status, 
```